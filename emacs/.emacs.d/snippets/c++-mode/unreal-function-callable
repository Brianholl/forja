# -*- mode: snippet -*-
# name: UFunction BlueprintCallable
# key: uff
# --
UFUNCTION(BlueprintCallable, Category="${1:`(save-match-data (if (string-match "Source/\\([^/]+\\)/" (buffer-file-name)) (match-string 1 (buffer-file-name)) "Gameplay"))`}")
${2:void} ${3:FunctionName}();