#+TITLE: Módulo Dashboard - Centro de Mando Gráfico
#+AUTHOR: Brian Hollweg
#+STARTUP: indent showeverything
#+PROPERTY: header-args:emacs-lisp :tangle yes :results silent

* 10. Dashboard y Pantalla de Inicio

Este módulo gestiona la interfaz gráfica que se presenta al iniciar Emacs, reemplazando el buffer de *scratch* estándar por un panel de control interactivo.

#+begin_src emacs-lisp
(use-package dashboard
  :ensure t
  :config
  (setq dashboard-startup-banner 'logo)
  (setq dashboard-center-content t)
  (setq dashboard-show-shortcuts nil)

  ;; --- Generador de botón manual (Prueba a prueba de fallos) ---
  (defun my/dashboard-insert-scratch (list-size)
    (insert "📝 Acciones Rápidas:\n")
    (widget-create 'push-button
                   :action (lambda (&rest ignore) (switch-to-buffer "*scratch*"))
                   :mouse-face 'highlight
                   :button-face 'dashboard-heading
                   "  [ Abrir Lienzo en Blanco (Scratch) ]")
    (insert "\n"))
  
  (add-to-list 'dashboard-item-generators '(scratch-button . my/dashboard-insert-scratch))

  ;; --- Contenido principal ---
  (setq dashboard-items '((recents  . 5)
                          (scratch-button . 1)
                          (projects . 5)
                          (agenda . 5)))

  ;; --- Notificación de actualizaciones disponibles ---
  (defvar my/forja-update-status nil
    "Estado de actualizaciones: nil (no chequeado), :checking, :available, :up-to-date, :error.")

  (defvar my/forja-dir
    (expand-file-name "forja/" "~/")
    "Directorio del repositorio FORJA.")

  (defun my/forja-check-updates ()
    "Chequea async si hay commits nuevos en el remote de FORJA."
    (when (and (file-directory-p (expand-file-name ".git" my/forja-dir))
               (executable-find "git"))
      (setq my/forja-update-status :checking)
      (let ((default-directory my/forja-dir))
        (set-process-sentinel
         (start-process "forja-fetch" nil "git" "fetch" "--quiet" "origin")
         (lambda (proc _event)
           (when (= (process-exit-status proc) 0)
             (let* ((default-directory my/forja-dir)
                    (behind (string-trim
                             (shell-command-to-string
                              "git rev-list HEAD..origin/main --count 2>/dev/null"))))
               (setq my/forja-update-status
                     (if (and (string-match-p "^[0-9]+$" behind)
                              (> (string-to-number behind) 0))
                         :available
                       :up-to-date))
               ;; Refrescar dashboard si está visible
               (when (get-buffer "*dashboard*")
                 (with-current-buffer "*dashboard*"
                   (dashboard-refresh-buffer))))))))))

  (defun my/dashboard-insert-updates (list-size)
    "Inserta notificación de actualizaciones en el dashboard."
    (pcase my/forja-update-status
      (:available
       (insert (propertize "  FORJA: hay actualizaciones disponibles. Ejecuta update.sh"
                           'face '(:foreground "#ff9800" :weight bold)))
       (insert "\n"))
      (:checking
       (insert (propertize "  Verificando actualizaciones..." 'face 'font-lock-comment-face))
       (insert "\n"))
      (_ nil)))

  (add-to-list 'dashboard-item-generators '(forja-updates . my/dashboard-insert-updates))
  (setq dashboard-items '((forja-updates . 1)
                          (recents  . 5)
                          (scratch-button . 1)
                          (projects . 5)
                          (agenda . 5)))

  ;; Chequear al iniciar (async, no bloquea)
  (add-hook 'emacs-startup-hook #'my/forja-check-updates)

  ;; IMPORTANTE: setup al final y activarlo
  (dashboard-setup-startup-hook)
  (setq initial-buffer-choice (lambda () (get-buffer-create "*dashboard*"))))
#+end_src
