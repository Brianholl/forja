# -*- mode: snippet -*-
# name: UE Log Fatal
# key: ulf
# --
UE_LOG(Log${1:`(save-match-data (if (string-match "Source/\\([^/]+\\)/" (buffer-file-name)) (match-string 1 (buffer-file-name)) "Temp"))`}, Fatal, TEXT("${2:Fatal Error}"));