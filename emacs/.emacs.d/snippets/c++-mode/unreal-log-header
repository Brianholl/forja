# -*- mode: snippet -*-
# name: Log Category Header
# key: ulh
# --
DECLARE_LOG_CATEGORY_EXTERN(Log${1:`(save-match-data (if (string-match "Source/\\([^/]+\\)/" (buffer-file-name)) (match-string 1 (buffer-file-name)) "MyGame"))`}, All, All);