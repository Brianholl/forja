#+TITLE: Módulo Web - JS, TS, HTML & CSS
#+AUTHOR: Brian Hollweg
#+STARTUP: indent showeverything
#+PROPERTY: header-args:emacs-lisp :tangle yes :results silent

* TABLA DE CONTENIDOS
- [[#10-entorno-de-desarrollo-web-full-stack][10. Entorno de Desarrollo Web (Full Stack)]]
  - [[#live-server--hot-reload][Live Server & Hot Reload]]
  - [[#compilar-y-ejecutar-nodejs][Compilar y Ejecutar (Node.js)]]

* 10. Entorno de Desarrollo Web (Full Stack)
Stack completo para desarrollo Full Stack.

#+begin_src emacs-lisp
;; --- 1. Tree-Sitter (Sintaxis Moderna) ---
(use-package emacs
  :ensure nil
  :config
  ;; Instalar gramáticas automáticamente
  (setq treesit-language-source-alist
        '((javascript "https://github.com/tree-sitter/tree-sitter-javascript")
          (typescript "https://github.com/tree-sitter/tree-sitter-typescript" "master" "typescript/src")
          (tsx "https://github.com/tree-sitter/tree-sitter-typescript" "master" "tsx/src")
          (json "https://github.com/tree-sitter/tree-sitter-json")
          (css "https://github.com/tree-sitter/tree-sitter-css")
          (html "https://github.com/tree-sitter/tree-sitter-html")))

  (dolist (lang '(javascript typescript tsx json css html))
    (unless (treesit-language-available-p lang)
      (condition-case err
          (treesit-install-language-grammar lang)
        (error (message "⚠️ No se pudo instalar gramática %s: %s" lang err)))))

  ;; Nivel máximo de coloreo tree-sitter (keywords, tipos, operadores, etc.)
  (setq treesit-font-lock-level 4)

  ;; Remapear modos antiguos a Tree-sitter
  (add-to-list 'major-mode-remap-alist '(js-mode . js-ts-mode))
  (add-to-list 'major-mode-remap-alist '(javascript-mode . js-ts-mode))
  (add-to-list 'major-mode-remap-alist '(typescript-mode . typescript-ts-mode))
  (add-to-list 'major-mode-remap-alist '(json-mode . json-ts-mode))
  (add-to-list 'major-mode-remap-alist '(css-mode . css-ts-mode))
  (add-to-list 'major-mode-remap-alist '(html-mode . html-ts-mode))
  (add-to-list 'major-mode-remap-alist '(mhtml-mode . html-ts-mode))

  ;; Asociar extensiones ANTES de web-mode
  (add-to-list 'auto-mode-alist '("\\.js\\'" . js-ts-mode))
  (add-to-list 'auto-mode-alist '("\\.mjs\\'" . js-ts-mode))
  (add-to-list 'auto-mode-alist '("\\.ts\\'" . typescript-ts-mode))
  (add-to-list 'auto-mode-alist '("\\.json\\'" . json-ts-mode))
  (add-to-list 'auto-mode-alist '("\\.css\\'" . css-ts-mode))
  (add-to-list 'auto-mode-alist '("\\.html\\'" . html-ts-mode))
  (add-to-list 'auto-mode-alist '("\\.tsx\\'" . tsx-ts-mode)))

;; Activar LSP
(add-hook 'js-ts-mode-hook 'lsp-deferred)
(add-hook 'typescript-ts-mode-hook 'lsp-deferred)
(add-hook 'json-ts-mode-hook 'lsp-deferred)
(add-hook 'css-ts-mode-hook 'lsp-deferred)
(add-hook 'html-ts-mode-hook 'lsp-deferred)

;; Fix: tree-sitter 0.26+ rechaza (:match "\\`PATTERN\\'" @cap) — anclas Emacs
;; inválidas en el motor regex de tree-sitter. Los features 'constant' y 'number'
;; de typescript-ts-mode built-in usan ese patrón y rompen TODO el font-lock
;; antes de que corran los hooks. Solución: reemplazar la función completa.
(with-eval-after-load 'typescript-ts-mode
  (defun typescript-ts-mode--font-lock-settings (language)
    (let ((func-exp (tsx-ts-mode--font-lock-compatibility-function-expression language)))
      (treesit-font-lock-rules

       :language language :feature 'comment
       `([(comment) (hash_bang_line)] @font-lock-comment-face)

       ;; 'constant' simplificado: solo true/false/null (sin (:match) ALL_CAPS)
       :language language :feature 'constant
       '([(true) (false) (null)] @font-lock-constant-face)

       :language language :feature 'keyword
       `([,@typescript-ts-mode--keywords] @font-lock-keyword-face
         [(this) (super)] @font-lock-keyword-face)

       :language language :feature 'string
       `((regex pattern: (regex_pattern)) @font-lock-regexp-face
         (string) @font-lock-string-face
         (template_string) @js--fontify-template-string
         (template_substitution ["${" "}"] @font-lock-misc-punctuation-face))

       :language language :override t :feature 'declaration
       `((,func-exp
          name: (identifier) @font-lock-function-name-face)
         (function_declaration
          name: (identifier) @font-lock-function-name-face)
         (function_signature
          name: (identifier) @font-lock-function-name-face)
         (method_definition
          name: (property_identifier) @font-lock-function-name-face)
         (method_signature
          name: (property_identifier) @font-lock-function-name-face)
         (required_parameter (identifier) @font-lock-variable-name-face)
         (optional_parameter (identifier) @font-lock-variable-name-face)
         (variable_declarator
          name: (identifier) @font-lock-function-name-face
          value: [(,func-exp) (arrow_function)])
         (variable_declarator
          name: (identifier) @font-lock-variable-name-face)
         (enum_declaration (identifier) @font-lock-type-face)
         (extends_clause value: (identifier) @font-lock-type-face)
         (extends_clause value: (member_expression
                                 object: (identifier) @font-lock-type-face
                                 property: (property_identifier) @font-lock-type-face))
         (arrow_function
          parameter: (identifier) @font-lock-variable-name-face)
         (variable_declarator
          name: (array_pattern
                 (identifier)
                 (identifier) @font-lock-function-name-face)
          value: (array (number) (,func-exp)))
         (catch_clause
          parameter: (identifier) @font-lock-variable-name-face)
         (import_clause (identifier) @font-lock-variable-name-face)
         (import_clause (named_imports (import_specifier
                                        alias: (identifier) @font-lock-variable-name-face)))
         (import_clause (named_imports (import_specifier
                                        !alias
                                        name: (identifier) @font-lock-variable-name-face)))
         (import_clause (namespace_import (identifier) @font-lock-variable-name-face)))

       :language language :feature 'identifier
       `((nested_type_identifier
          module: (identifier) @font-lock-type-face)
         (type_identifier) @font-lock-type-face
         (predefined_type) @font-lock-type-face
         (new_expression
          constructor: (identifier) @font-lock-type-face)
         (enum_body (property_identifier) @font-lock-type-face)
         (enum_assignment name: (property_identifier) @font-lock-type-face)
         (variable_declarator
          name: (identifier) @font-lock-variable-name-face)
         (for_in_statement
          left: (identifier) @font-lock-variable-name-face)
         (arrow_function
          parameters:
          [(_ (identifier) @font-lock-variable-name-face)
           (_ (_ (identifier) @font-lock-variable-name-face))
           (_ (_ (_ (identifier) @font-lock-variable-name-face)))]))

       :language language :feature 'property
       `((property_signature
          name: (property_identifier) @font-lock-property-name-face)
         (public_field_definition
          name: (property_identifier) @font-lock-property-name-face)
         (pair key: (property_identifier) @font-lock-property-use-face)
         ((shorthand_property_identifier) @font-lock-property-use-face))

       :language language :feature 'expression
       `((assignment_expression
          left: [(identifier) @font-lock-function-name-face
                 (member_expression
                  property: (property_identifier) @font-lock-function-name-face)]
          right: [(,func-exp) (arrow_function)]))

       :language language :feature 'function
       '((call_expression
          function:
          [(identifier) @font-lock-function-call-face
           (member_expression
            property: (property_identifier) @font-lock-function-call-face)]))

       :language language :feature 'pattern
       `((pair_pattern
          key: (property_identifier) @font-lock-property-use-face
          value: [(identifier) @font-lock-variable-name-face
                  (assignment_pattern left: (identifier) @font-lock-variable-name-face)])
         (array_pattern (identifier) @font-lock-variable-name-face)
         ((shorthand_property_identifier_pattern) @font-lock-variable-name-face))

       :language language :feature 'jsx
       (append (tsx-ts-mode--font-lock-compatibility-bb1f97b language)
               `((jsx_attribute (property_identifier) @typescript-ts-jsx-attribute-face)))

       ;; 'number' simplificado: solo literales numéricos (sin (:match) NaN/Infinity)
       :language language :feature 'number
       '((number) @font-lock-number-face)

       :language language :feature 'operator
       `([,@typescript-ts-mode--operators] @font-lock-operator-face
         (ternary_expression ["?" ":"] @font-lock-operator-face))

       :language language :feature 'bracket
       '((["(" ")" "[" "]" "{" "}"]) @font-lock-bracket-face)

       :language language :feature 'delimiter
       '((["," "." ";" ":"]) @font-lock-delimiter-face)

       :language language :override t :feature 'escape-sequence
       '((escape_sequence) @font-lock-escape-face)))))

(add-hook 'typescript-ts-mode-hook
  (lambda () (setq treesit-font-lock-level 4) (treesit-font-lock-recompute-features)))
(add-hook 'tsx-ts-mode-hook
  (lambda () (setq treesit-font-lock-level 4) (treesit-font-lock-recompute-features)))
(add-hook 'js-ts-mode-hook
  (lambda () (setq treesit-font-lock-level 4) (treesit-font-lock-recompute-features)))

;; --- 2. Prettier (Formateo Automático) ---
(use-package prettier-js
  :hook ((js-ts-mode . prettier-js-mode)
         (typescript-ts-mode . prettier-js-mode)
         (web-mode . prettier-js-mode)
         (css-ts-mode . prettier-js-mode)))

;; --- 3. Web-Mode (Solo para JSX/TSX) ---
(use-package web-mode
  :mode (("\\.jsx\\'" . web-mode)
         ;; ("\\.tsx\\'" . web-mode)
         ("\\.vue\\'" . web-mode))
  :config
  (setq web-mode-markup-indent-offset 2)
  (setq web-mode-css-indent-offset 2)
  (setq web-mode-code-indent-offset 2)
  (setq web-mode-enable-css-colorization t)
  (setq web-mode-enable-auto-pairing t)
  (setq web-mode-enable-current-element-highlight t))

(add-hook 'web-mode-hook 'lsp-deferred)

;; --- 4. Emmet (HTML rápido) ---
(use-package emmet-mode
  :hook ((html-ts-mode . emmet-mode)
         (css-ts-mode . emmet-mode)
         (web-mode . emmet-mode))
  :bind (:map emmet-mode-keymap
              ("TAB" . emmet-expand-line)
              ("<tab>" . emmet-expand-line)))

;; --- 5. Node.js REPL ---
(use-package nodejs-repl
  :bind ("C-c C-n" . nodejs-repl))

;; --- 6. Utilidades Visuales ---
(use-package rainbow-mode
  :hook (prog-mode . rainbow-mode))

;; Resaltar pares de paréntesis
(use-package rainbow-delimiters
  :hook (prog-mode . rainbow-delimiters-mode))
#+end_src

** Live Server & Hot Reload

#+begin_src emacs-lisp
(defun my/run-live-server ()
  "Lanza live-server npm para el proyecto actual o el directorio del archivo.
En Termux no abre browser (no hay Firefox); en PC abre Firefox."
  (interactive)
  (ignore-errors (kill-process (get-process "live-server-proc")))
  (let ((default-directory
         ;; Usa la raíz del proyecto si existe, sino usa el directorio del archivo actual
         (or (and (bound-and-true-p projectile-mode) (projectile-project-root))
             (file-name-directory buffer-file-name))))
    (if default-directory
        (let ((args (if (bound-and-true-p my/is-termux)
                        '("--quiet" "--no-browser")
                      '("--quiet" "--browser=firefox"))))
          (apply #'start-process "live-server-proc"
                 "*live-server-log*" "live-server" args)
          (message "🚀 Live server iniciado en: %s%s"
                   default-directory
                   (if (bound-and-true-p my/is-termux)
                       " (abrí http://localhost:8080 en el navegador)"
                     "")))
      (message "❌ No se pudo determinar el directorio para live-server"))))
#+end_src

** Compilar y Ejecutar (Node.js)

#+begin_src emacs-lisp
(defun my/node-run ()
  "Ejecuta el proyecto Node.js actual.
Detecta Express/package.json (npm start), o ejecuta el archivo directamente."
  (interactive)
  (let* ((project-root (or (and (fboundp 'projectile-project-root) (projectile-project-root))
                           (locate-dominating-file default-directory "package.json")
                           default-directory))
         (default-directory project-root)
         (pkg-json (expand-file-name "package.json" project-root)))
    (cond
     ;; Proyecto con package.json y script "start" o "dev"
     ((file-exists-p pkg-json)
      (let* ((pkg-content (with-temp-buffer
                            (insert-file-contents pkg-json)
                            (buffer-string)))
             (has-dev (string-match-p "\"dev\"" pkg-content))
             (has-start (string-match-p "\"start\"" pkg-content)))
        (cond
         (has-dev   (compile "npm run dev"))
         (has-start (compile "npm start"))
         ;; package.json sin scripts → buscar entry point
         (t (my/node--run-file project-root)))))
     ;; Archivo suelto
     ((buffer-file-name)
      (save-buffer)
      (compile (format "node %s" (file-name-nondirectory buffer-file-name))))
     (t (message "❌ No se encontró archivo JS para ejecutar")))))

(defun my/node--run-file (project-root)
  "Busca y ejecuta el entry point del proyecto Node.js / TypeScript."
  (let ((candidates '("src/index.ts" "src/index.js" "index.ts"
                      "index.js" "app.js" "server.js" "src/app.js")))
    (catch 'found
      (dolist (file candidates)
        (when (file-exists-p (expand-file-name file project-root))
          (compile (if (and (string-match-p "\\.ts$" file) (executable-find "tsx"))
                       (format "tsx %s" file)
                     (format "node %s" file)))
          (throw 'found t)))
      ;; Fallback: ejecutar archivo actual
      (if (buffer-file-name)
          (let ((f (file-name-nondirectory buffer-file-name)))
            (compile (if (and (string-match-p "\\.ts$" f) (executable-find "tsx"))
                         (format "tsx %s" f)
                       (format "node %s" f))))
        (message "❌ No se encontró entry point")))))

(defun my/node-test ()
  "Ejecuta tests del proyecto Node.js.
Detecta jest, mocha, o npm test."
  (interactive)
  (let* ((project-root (or (and (fboundp 'projectile-project-root) (projectile-project-root))
                           (locate-dominating-file default-directory "package.json")
                           default-directory))
         (default-directory project-root))
    (cond
     ;; Jest
     ((file-exists-p (expand-file-name "jest.config.js" project-root))
      (compile "npx jest --verbose"))
     ((file-exists-p (expand-file-name "jest.config.ts" project-root))
      (compile "npx jest --verbose"))
     ;; package.json con script "test"
     ((and (file-exists-p (expand-file-name "package.json" project-root))
           (with-temp-buffer
             (insert-file-contents (expand-file-name "package.json" project-root))
             (string-match-p "\"test\"" (buffer-string))))
      (compile "npm test"))
     ;; Fallback
     (t (compile "npm test")))))

(defun my/node-format-buffer ()
  "Formatea el buffer JS/TS con prettier (si disponible) o lsp."
  (interactive)
  (if (and (fboundp 'prettier-js) (executable-find "prettier"))
      (prettier-js)
    (lsp-format-buffer)))

(defun my/node-keybindings ()
  "Atajos locales para desarrollo en Node.js/Express."
  (local-set-key (kbd "<f5>") #'my/node-run)
  (local-set-key (kbd "C-c C-t") #'my/node-test)
  (local-set-key (kbd "C-c C-f") #'my/node-format-buffer))

;; F5 en JS/TS usa my/node-run (detecta proyecto Node.js)
(add-hook 'js-ts-mode-hook #'my/node-keybindings)
(add-hook 'typescript-ts-mode-hook #'my/node-keybindings)

;; HTML/CSS sigue usando live-server
(add-hook 'html-ts-mode-hook
          (lambda () (local-set-key (kbd "<f5>") 'my/run-live-server)))
(add-hook 'css-ts-mode-hook
          (lambda () (local-set-key (kbd "<f5>") 'my/run-live-server)))
#+end_src

* 20. TypeScript Standalone (CLI y Librería)

Proyectos TypeScript puros sin stack web. Usa =tsx= para ejecutar sin compilar a JS.
Requiere: =sudo npm install -g tsx typescript=

#+begin_src emacs-lisp
(defun my/new-ts-project (name)
  "Crea un proyecto TypeScript standalone: CLI o librería npm.
Pregunta el tipo con completing-read."
  (interactive "sNombre del proyecto: ")
  (let* ((tipo (completing-read "Tipo de proyecto: " '("cli" "libreria") nil t))
         (base-dir (expand-file-name name (concat (my/mu-projects-base) "ts/")))
         (src-dir  (concat base-dir "/src")))

    (make-directory src-dir t)

    ;; tsconfig.json — base estricto para ambos tipos
    (with-temp-file (concat base-dir "/tsconfig.json")
      (insert "{\n")
      (insert "  \"compilerOptions\": {\n")
      (insert "    \"target\": \"ES2022\",\n")
      (insert "    \"module\": \"commonjs\",\n")
      (insert "    \"lib\": [\"ES2022\"],\n")
      (insert "    \"outDir\": \"./dist\",\n")
      (insert "    \"rootDir\": \"./src\",\n")
      (insert "    \"strict\": true,\n")
      (insert "    \"esModuleInterop\": true,\n")
      (insert "    \"skipLibCheck\": true,\n")
      (insert "    \"declaration\": true,\n")
      (insert "    \"declarationMap\": true,\n")
      (insert "    \"sourceMap\": true\n")
      (insert "  },\n")
      (insert "  \"include\": [\"src/**/*\"],\n")
      (insert "  \"exclude\": [\"node_modules\", \"dist\"]\n")
      (insert "}\n"))

    (if (string= tipo "cli")
        (my/new-ts--cli name base-dir src-dir)
      (my/new-ts--lib name base-dir src-dir))

    ;; .gitignore y .projectile comunes
    (with-temp-file (concat base-dir "/.gitignore")
      (insert "node_modules/\ndist/\n*.js.map\n"))
    (with-temp-file (concat base-dir "/.projectile") (insert ""))

    (my/git-init-and-commit base-dir name)
    ;; npm install en background — tsx y typescript quedan en node_modules/.bin/
    (let ((default-directory base-dir))
      (start-process "npm-install" "*npm-install*" "npm" "install"))
    (message "Proyecto TypeScript '%s' (%s) creado. Instalando deps en background..." name tipo)
    (find-file (concat src-dir "/index.ts"))))

(defun my/new-ts--cli (name base-dir src-dir)
  "Genera template para herramienta CLI en TypeScript."
  ;; package.json con bin y scripts tsx
  (with-temp-file (concat base-dir "/package.json")
    (insert (format "{\n  \"name\": \"%s\",\n" name))
    (insert "  \"version\": \"0.1.0\",\n")
    (insert "  \"description\": \"\",\n")
    (insert "  \"main\": \"dist/index.js\",\n")
    (insert (format "  \"bin\": { \"%s\": \"dist/index.js\" },\n" name))
    (insert "  \"scripts\": {\n")
    (insert "    \"build\": \"tsc\",\n")
    (insert (format "    \"start\": \"tsx src/index.ts\",\n"))
    (insert (format "    \"dev\":   \"tsx watch src/index.ts\",\n"))
    (insert "    \"test\":  \"jest\"\n")
    (insert "  },\n")
    (insert "  \"devDependencies\": {\n")
    (insert "    \"typescript\": \"^5\",\n")
    (insert "    \"@types/node\": \"^20\",\n")
    (insert "    \"tsx\": \"^4\"\n")
    (insert "  }\n")
    (insert "}\n"))
  ;; src/index.ts — entrada CLI con process.argv
  (with-temp-file (concat src-dir "/index.ts")
    (insert "#!/usr/bin/env node\n")
    (insert "// -*- mode: typescript-ts -*-\n\n")
    (insert (format "const nombre = \"%s\";\n" name))
    (insert "const args = process.argv.slice(2);\n\n")
    (insert "if (args.length === 0) {\n")
    (insert (format "  console.log(`%s — sin argumentos`);\n" name))
    (insert "  process.exit(0);\n")
    (insert "}\n\n")
    (insert "console.log(`Args: ${args.join(', ')}`);\n")))

(defun my/new-ts--lib (name base-dir src-dir)
  "Genera template para librería npm en TypeScript."
  (with-temp-file (concat base-dir "/package.json")
    (insert (format "{\n  \"name\": \"%s\",\n" name))
    (insert "  \"version\": \"0.1.0\",\n")
    (insert "  \"description\": \"\",\n")
    (insert "  \"main\": \"dist/index.js\",\n")
    (insert "  \"types\": \"dist/index.d.ts\",\n")
    (insert "  \"files\": [\"dist\"],\n")
    (insert "  \"scripts\": {\n")
    (insert "    \"build\": \"tsc\",\n")
    (insert (format "    \"dev\":   \"tsx watch src/index.ts\",\n"))
    (insert "    \"test\":  \"jest\"\n")
    (insert "  },\n")
    (insert "  \"devDependencies\": {\n")
    (insert "    \"typescript\": \"^5\",\n")
    (insert "    \"@types/node\": \"^20\",\n")
    (insert "    \"tsx\": \"^4\"\n")
    (insert "  }\n")
    (insert "}\n"))
  ;; src/index.ts — exports de la librería
  (with-temp-file (concat src-dir "/index.ts")
    (insert (format "// %s — punto de entrada de la librería\n\n" name))
    (insert (format "export function saludo(nombre: string): string {\n"))
    (insert (format "  return `Hola desde %s, ${nombre}!`;\n" name))
    (insert "}\n")))

#+end_src

* 30. Generadores de Proyecto

#+begin_src emacs-lisp
(defun my/new-web-project (name)
  "Crea estructura básica para un proyecto web frontend."
  (interactive "sNombre del proyecto: ")
  (let ((project-dir (expand-file-name name (concat (my/mu-projects-base) "web/"))))
    (make-directory project-dir t)
    (make-directory (concat project-dir "/css") t)
    (make-directory (concat project-dir "/js") t)
    (make-directory (concat project-dir "/assets/images") t)
    (with-temp-file (concat project-dir "/index.html")
      (insert "<!DOCTYPE html>\n<html lang=\"es\">\n<head>\n")
      (insert "    <meta charset=\"UTF-8\">\n")
      (insert "    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\">\n")
      (insert (format "    <title>%s</title>\n" name))
      (insert "    <link rel=\"stylesheet\" href=\"css/style.css\">\n")
      (insert "</head>\n<body>\n")
      (insert (format "    <h1>¡Bienvenido a %s!</h1>\n" name))
      (insert "    <p>Proyecto creado desde Emacs 🚀</p>\n")
      (insert "    <script src=\"js/script.js\"></script>\n</body>\n</html>\n"))
    (with-temp-file (concat project-dir "/css/style.css")
      (insert "* { margin: 0; padding: 0; box-sizing: border-box; }\n")
      (insert "body {\n    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;\n")
      (insert "    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);\n")
      (insert "    min-height: 100vh; display: flex; flex-direction: column;\n")
      (insert "    align-items: center; justify-content: center;\n}\n")
      (insert "h1 { color: white; font-size: 3rem; text-shadow: 2px 2px 4px rgba(0,0,0,0.3); }\n")
      (insert "p  { color: white; font-size: 1.2rem; }\n"))
    (with-temp-file (concat project-dir "/js/script.js")
      (insert (format "console.log('¡%s cargado!');\n" name))
      (insert "document.addEventListener('DOMContentLoaded', () => {});\n"))
    (with-temp-file (concat project-dir "/.gitignore")
      (insert "node_modules/\n.DS_Store\n"))
    (with-temp-file (concat project-dir "/.projectile") (insert ""))
    (my/git-init-and-commit project-dir name)
    (message "🌐 Proyecto web creado: %s" project-dir)
    (find-file (concat project-dir "/index.html"))))

(defun my/new-web-tailwind-project (name)
  "Crea proyecto web con Tailwind CSS via CDN."
  (interactive "sNombre del proyecto: ")
  (let ((project-dir (expand-file-name name (concat (my/mu-projects-base) "web/"))))
    (make-directory project-dir t)
    (make-directory (concat project-dir "/js") t)
    (with-temp-file (concat project-dir "/index.html")
      (insert "<!DOCTYPE html>\n<html lang=\"es\">\n<head>\n")
      (insert "    <meta charset=\"UTF-8\">\n")
      (insert "    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\">\n")
      (insert (format "    <title>%s</title>\n" name))
      (insert "    <script src=\"https://cdn.tailwindcss.com\"></script>\n</head>\n")
      (insert "<body class=\"bg-gradient-to-br from-purple-600 to-blue-500 min-h-screen flex items-center justify-center\">\n")
      (insert "    <div class=\"text-center\">\n")
      (insert (format "        <h1 class=\"text-6xl font-bold text-white mb-4\">%s</h1>\n" name))
      (insert "        <p class=\"text-xl text-white\">Proyecto creado desde Emacs con Tailwind CSS 🎨</p>\n")
      (insert "    </div>\n    <script src=\"js/script.js\"></script>\n</body>\n</html>\n"))
    (with-temp-file (concat project-dir "/js/script.js")
      (insert (format "console.log('¡%s con Tailwind CSS!');\n" name)))
    (with-temp-file (concat project-dir "/.gitignore")
      (insert "node_modules/\n.DS_Store\n"))
    (with-temp-file (concat project-dir "/.projectile") (insert ""))
    (my/git-init-and-commit project-dir name)
    (message "🎨 Proyecto Tailwind creado: %s" project-dir)
    (find-file (concat project-dir "/index.html"))))

(defun my/new-express-project (name)
  "Crea un nuevo proyecto Node.js con Express para REST API."
  (interactive "sNombre del proyecto: ")
  (let ((project-dir (expand-file-name name (concat (my/mu-projects-base) "node/"))))
    (make-directory project-dir t)
    (with-temp-file (concat project-dir "/package.json")
      (insert (format "{\n  \"name\": \"%s\",\n  \"version\": \"1.0.0\",\n" name))
      (insert "  \"main\": \"src/index.js\",\n")
      (insert "  \"scripts\": {\n    \"start\": \"node src/index.js\",\n")
      (insert "    \"dev\": \"node --watch src/index.js\"\n  },\n")
      (insert "  \"dependencies\": {\n")
      (insert "    \"express\": \"^4.18.0\",\n    \"cors\": \"^2.8.5\",\n    \"dotenv\": \"^16.0.0\"\n  }\n}\n"))
    (make-directory (concat project-dir "/src/routes") t)
    (with-temp-file (concat project-dir "/src/index.js")
      (insert "require('dotenv').config();\n")
      (insert "const express = require('express');\n")
      (insert "const cors = require('cors');\n")
      (insert "const apiRoutes = require('./routes/api');\n\n")
      (insert "const app = express();\n")
      (insert "const PORT = process.env.PORT || 3000;\n\n")
      (insert "app.use(cors());\napp.use(express.json());\n")
      (insert "app.use('/api', apiRoutes);\n\n")
      (insert "app.get('/', (req, res) => {\n")
      (insert (format "  res.json({ message: 'Hola desde %s' });\n});\n\n" name))
      (insert "app.get('/health', (req, res) => { res.json({ status: 'ok' }); });\n\n")
      (insert "app.listen(PORT, () => {\n")
      (insert "  console.log(`🚀 Server corriendo en http://localhost:${PORT}`);\n});\n"))
    (with-temp-file (concat project-dir "/src/routes/api.js")
      (insert "const express = require('express');\n")
      (insert "const router = express.Router();\n\n")
      (insert "router.get('/items', (req, res) => { res.json({ items: [] }); });\n")
      (insert "router.post('/items', (req, res) => {\n")
      (insert "  const { name } = req.body;\n")
      (insert "  res.status(201).json({ message: `Item ${name} creado` });\n});\n\n")
      (insert "module.exports = router;\n"))
    (with-temp-file (concat project-dir "/.env")
      (insert "PORT=3000\nNODE_ENV=development\n"))
    (with-temp-file (concat project-dir "/.gitignore")
      (insert "node_modules/\n.env\n*.log\n"))
    (let ((default-directory project-dir))
      (async-shell-command "npm install" "*npm-install*"))
    (with-temp-file (concat project-dir "/.projectile") (insert ""))
    (my/git-init-and-commit project-dir name)
    (message "🟢 Proyecto Express creado: %s" project-dir)
    (find-file (concat project-dir "/src/index.js"))))

(my/forja-register-lang
 :modes   '(js-mode js-ts-mode typescript-mode typescript-ts-mode tsx-ts-mode)
 :run     'my/node-run          :run-fallback    "node ."
 :build   nil                   :build-fallback  "npm install"
 :test    'my/node-test         :test-fallback   "npm test"
 :format  'my/node-format-buffer :format-fallback nil)

(my/forja-register-lang
 :modes   '(html-mode html-ts-mode mhtml-mode css-mode css-ts-mode web-mode)
 :run     'my/run-live-server   :run-fallback    "python -m http.server"
 :build   nil                   :build-fallback  "npm install"
 :test    nil                   :test-fallback   nil
 :format  nil                   :format-fallback nil)
#+end_src

